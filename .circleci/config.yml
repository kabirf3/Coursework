version: 2.1

jobs:
  hello-job:
    docker:
      - image: cimg/node:17.2.0               # the primary container
    steps:
      - checkout                              # check out the code in the project directory
      - run: echo "hello world"               # print 'hello world'

workflows:
  my-workflow:                                # Name of the workflow.  can add additional workflows below.
    jobs:                                     # The jobs to run as part of this workflow
      - hello-job
    filters:
      branches:
        only:
          - main  # main branch 

# This config was automatically generated from your source code
# Stacks detected: deps:node:src
#version: 2.1
#orbs:
 # node: circleci/node@5
#jobs:
  #test-node:
    # Install node dependencies and run tests
    #executor: node/default
    #working_directory: ~/project/src
   # steps:
      - #checkout:
         # path: ~/projectnd
      - #node/install-packages:
         # pkg-manager: npm
      - #run:
          #name: Run tests
         # command: echo \"No test specified in package.json\"
  #deploy:
    # This is an example deploy job, not actually used by the workflow
    #docker:
     # - image: cimg/base:stable
    #steps:
      # Replace this with steps to deploy to users
      - #run:
          #name: deploy
          #command: '#e.g. ./deploy.sh'
#workflows:
 # build-and-test:
    #jobs: 
    #  - test-node
    # - deploy:
    #     requires:
    #       - test-node 